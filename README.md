# Project 3 - Estimating Pi using Monte Carlo

This project estimates the value of pi using the Monte Carlo simulation method in Python.

## How it works

Random points are generated inside a square.
If the points fall inside the circle, they are counted.
The ratio is used to estimate pi.
